spring:
  datasource:
    url: jdbc:h2:mem:customer-api-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.xml
  h2:
    console:
      enabled: false
