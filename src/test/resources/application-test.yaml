spring:
  liquibase:
    enabled: false
  output:
    ansi:
      enabled: always
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    properties:
      generate-ddl: true
    hibernate:
      ddl-auto: create-drop
    show-sql: false


